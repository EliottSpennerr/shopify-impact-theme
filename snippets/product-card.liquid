{%- comment -%}
----------------------------------------------------------------------------------------------------------------------
PRODUCT CARD COMPONENT
----------------------------------------------------------------------------------------------------------------------

This component is used in collection and featured collection to render a single product as a card

********************************************
Supported variables
********************************************

* product: the product to render
* show_rating: show or not the rating. If nothing is set, the theme uses the default product card setting
* show_vendor: show or not the vendor. If nothing is set, the theme uses the default product card setting
* show_quick_buy: show or not the quick buy. If nothing is set, the theme uses the default product card setting
* show_secondary_image: show or not the secondary image. If nothing is set, the theme uses the default product card setting
* show_swatches: allow to force hiding swatches. If nothing is set, it will default to the default card strategy
* stacked: indicate if the product is in stack mode
* reveal: if set to true the item will be revealed in a stacked mode
* background: the background to use for the product card (if none is passed then the one from the global setting is used)
* text_color: the text color to use for the product card (if none is passed then the one from the global setting is used)
* text_alignment: can be "center". If nothing is set, the theme uses the default product card setting

Typography CSS variables (set by calling section):
  --pc-vendor-size     vendor font size (default 12px)
  --pc-vendor-weight   vendor font weight (default 500)
  --pc-vendor-family   vendor font family (default inherit)
  --pc-title-size      title font size (default 15px)
  --pc-title-weight    title font weight (default 700)
  --pc-title-family    title font family (default inherit)
  --pc-stock-size      stock label font size (default 11px)
  --pc-stock-weight    stock label font weight (default 700)
{%- endcomment -%}

{%- assign show_rating = show_rating | default: settings.show_product_rating, allow_false: true -%}
{%- assign show_vendor = show_vendor | default: settings.show_vendor, allow_false: true -%}
{%- assign show_quick_buy = show_quick_buy | default: settings.show_quick_buy, allow_false: true -%}
{%- assign show_secondary_image = show_secondary_image | default: settings.show_secondary_image, allow_false: true -%}
{%- assign text_alignment = text_alignment | default: settings.product_info_alignment -%}

{%- if stacked and section.settings.products_per_row_mobile == '2' -%}
  {%- assign mobile_reduced = true -%}
{%- endif -%}

{%- assign section_background = section.settings.background_gradient | default: section.settings.background | default: settings.background -%}
{%- assign card_background = background | default: settings.product_card_background -%}
{%- assign card_text_color = text_color | default: settings.product_card_text_color -%}

{%- if section_background != 'rgba(0,0,0,0)' and card_background != 'rgba(0,0,0,0)' and section_background != card_background -%}
  {%- assign card_class = 'product-card ' -%}
{%- else -%}
  {%- assign card_class = 'product-card product-card--blends ' -%}
{%- endif -%}

{%- if show_secondary_image and product.media.size > 1 -%}
  {%- assign card_class = card_class | append: 'product-card--show-secondary-media' -%}
{%- endif -%}

{%- comment -%}
  ─── STJÄRNBETYG: förberäkning ──────────────────────────────────────
  Shopifys rating-metafält returnerar ett Rating-objekt. floor-jämförelser
  på det objektet kan ge fel resultat. Säkrare metod: multiplicera med 2,
  runda till närmaste heltal → halvstjärneenheter (0–10).
  I loopen: i_x2 = i*2. Fylld om pc_x2 >= i_x2. Halv om pc_x2 == i_x2-1.
  Tom annars. Gradient-ID: product.id + i = inga krockar.
{%- endcomment -%}
{%- assign pc_rv = product.metafields.reviews.rating.value.rating | plus: 0.0 -%}
{%- assign pc_rc = product.metafields.reviews.rating_count.value | plus: 0 -%}
{%- assign pc_x2 = pc_rv | times: 2 | round -%}

<style>
  /* ── Produktkort: info-layout ──────────────────────────────────────── */
  .product-card__info-stack {
    display: flex;
    flex-direction: column;
    gap: 5px;
    padding: 12px 4px 8px;
  }

  /* Leverantör — typsnitt styrs via CSS-variabler fran kallande sektion */
  .product-card__vendor-label {
    font-size: var(--pc-vendor-size, 10px);
    font-weight: var(--pc-vendor-weight, 300);
    font-family: var(--pc-vendor-family, inherit);
    opacity: 0.5;
    text-transform: uppercase;
    letter-spacing: 0.04em;
    line-height: 1;
    display: block;
  }

  /* Titel */
  .product-card__title-link {
    font-size: var(--pc-title-size, 18px);
    font-weight: var(--pc-title-weight, 600);
    font-family: var(--pc-title-family, inherit);
    text-decoration: none;
    color: currentColor;
    display: block;
    line-height: 1.3;
    padding-bottom: 10px
  }

  .product-card__title-link:hover { opacity: 0.75; }

  /* Lagerstatus */
  .product-card__stock {
    display: flex;
    align-items: center;
    gap: 6px;
    margin-top: 0px;
  }

  .product-card__stock-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    flex-shrink: 0;
       margin-bottom: -3px;
  }

  .product-card__stock-dot--in  { background: #22c55e; }
  .product-card__stock-dot--out { background: #ef4444; }

  .product-card__stock-text {
    font-size: var(--pc-stock-size, 10px);
    font-weight: var(--pc-stock-weight, 300);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    opacity: 0.65;
    margin-bottom: -5px;
  }

  /* Stjärnor */
  .product-card__stars {
    display: flex;
    align-items: center;
    gap: 2px;
    flex-wrap: nowrap;
    margin-top: 0px;
  }

  .product-card__stars svg { display: block; flex-shrink: 0; }

  .product-card__rating-text {
    font-size: 12px;
    opacity: 0.55;
    margin-left: 4px;
    white-space: nowrap;
  }

  /* Pris + Köp-rad */
  .product-card__price-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    margin-top: 3px;
  }

  /* ── Köp-knapp ─────────────────────────────────────────────────────── */
  .product-card__buy-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 18px;
    border-radius: 100px;
    background: #1a2e44;
    color: #fff;
    font-size: 13px;
    font-weight: 600;
    font-family: inherit;
    border: none;
    cursor: pointer;
    text-decoration: none;
    white-space: nowrap;
    transition: opacity 0.15s ease, background 0.3s ease;
    flex-shrink: 0;
    line-height: 1;
    position: relative;
    overflow: hidden;
    min-width: 64px;
  }

  .product-card__buy-btn:hover { background: #025dc6; }

  .product-card__buy-btn--disabled {
    opacity: 0.35;
    cursor: default;
    pointer-events: none;
  }

  /* ── Knappens inre element ─────────────────────────────────────────── */
  .product-card__buy-btn .btn-label,
  .product-card__buy-btn .btn-spinner,
  .product-card__buy-btn .btn-check {
    transition: opacity 0.18s ease, transform 0.18s ease;
  }

  .product-card__buy-btn .btn-spinner,
  .product-card__buy-btn .btn-check {
    position: absolute;
    left: 50%;
    top: 50%;
    transform: translate(-50%, -50%);
    opacity: 0;
    pointer-events: none;
  }

  /* ── Laddningsläge ─────────────────────────────────────────────────── */
  .product-card__buy-btn.is-loading {
    pointer-events: none;
  }

  .product-card__buy-btn.is-loading .btn-label {
    opacity: 0;
    transform: scale(0.7);
  }

  .product-card__buy-btn.is-loading .btn-spinner {
    opacity: 1;
    transform: translate(-50%, -50%) rotate(0deg);
  }

  @keyframes pc-btn-spin {
    to { transform: translate(-50%, -50%) rotate(360deg); }
  }

  .product-card__buy-btn.is-loading .btn-spinner {
    animation: pc-btn-spin 0.65s linear infinite;
  }

  /* ── Succéläge ─────────────────────────────────────────────────────── */
  .product-card__buy-btn.is-success {
    background: #025dc6;
    pointer-events: none;
  }

  .product-card__buy-btn.is-success .btn-label {
    opacity: 0;
    transform: scale(0.7);
  }

  .product-card__buy-btn.is-success .btn-check {
    opacity: 1;
  }

  @keyframes pc-check-pop {
    0%   { transform: translate(-50%, -50%) scale(0.5); opacity: 0; }
    60%  { transform: translate(-50%, -50%) scale(1.15); opacity: 1; }
    100% { transform: translate(-50%, -50%) scale(1); opacity: 1; }
  }

  .product-card__buy-btn.is-success .btn-check {
    animation: pc-check-pop 0.3s ease forwards;
  }

  /* ── Produktbild: border-radius alla hörn ──────────────────────────
     Temats CSS ger vanligtvis border-radius bara på toppen (korten har
     rak botten mot info-sektionen). Här tvingar vi full border-radius
     på figuren med overflow:hidden så bilden klipps lika på alla kanter.
  ────────────────────────────────────────────────────────────────────── */
  .product-card__figure {
    border-radius: var(--border-radius, 12px);
    overflow: hidden;
  }
</style>

<product-card handle="{{ product.handle | escape }}" {% if reveal %}reveal-js{% endif %} {% render 'surface', class: card_class, background: card_background, text_color: card_text_color %}>

  {%- comment -%}──────────────────────────────────
    BADGES
  ──────────────────────────────────{%- endcomment -%}
  {%- if show_badges and product.media.size > 0 -%}
    {%- render 'product-badges', product: product, class: 'product-card__badge-list' -%}
  {%- endif -%}

  {%- comment -%}──────────────────────────────────
    PRODUKTBILD
  ──────────────────────────────────{%- endcomment -%}
  {%- if product.media.size > 0 -%}
    <div class="product-card__figure">
      <a href="{{ product.url }}" data-instant>
        {%- capture sizes -%}
          {%- if stacked -%}
            (max-width: 699px) calc(100vw / {{ section.settings.products_per_row_mobile }} - 40px), (max-width: 1200px) calc(100vw / {{ 3 | at_most: section.settings.products_per_row_desktop }} - 64px), calc(min(100vw - 96px, {{ settings.page_width }}px) / {{ section.settings.products_per_row_desktop }} - (24px / {{ section.settings.products_per_row_desktop }} * {{ section.settings.products_per_row_desktop | minus: 1 }}))
          {%- else -%}
            (max-width: 699px) 74vw, (max-width: 999px) 38vw, calc(min(100vw - 96px, {{ settings.page_width }}px) / {{ section.settings.products_per_row_desktop }} - (24px / {{ section.settings.products_per_row_desktop }} * {{ section.settings.products_per_row_desktop | minus: 1 }}))
          {%- endif -%}
        {%- endcapture -%}

        {%- capture main_image_classes -%}product-card__image product-card__image--primary {% if settings.product_image_aspect_ratio contains 'crop' %}object-fill-safe{% endif %} aspect-{{ settings.product_image_aspect_ratio | split: '_' | first }}{%- endcapture -%}
        {{- product.featured_media | image_url: width: product.featured_media.width | image_tag: loading: 'lazy', sizes: sizes, widths: '200,300,400,500,600,700,800,1000,1200,1400,1600,1800', class: main_image_classes -}}

        {%- if show_secondary_image and product.media.size > 1 -%}
          {%- assign next_media = product.media[product.featured_media.position] | default: product.media[1] -%}
          {{- next_media | image_url: width: next_media.width | image_tag: class: 'product-card__image product-card__image--secondary object-fill', loading: 'lazy', fetchpriority: 'low', sizes: sizes, widths: '200,300,400,500,600,700,800,1000,1200,1400,1600,1800' -}}
        {%- endif -%}
      </a>

      {%- if show_quick_buy and product.available -%}
        <div class="product-card__quick-buy">
          {%- assign quick_add_label = 'product.general.quick_add' | t -%}
          {%- if product.variants.size == 1 and product.selling_plan_groups.size == 0 -%}
            {%- form 'product', product, is: 'product-form' -%}
              <input type="hidden" name="id" value="{{ product.selected_or_first_available_variant.id }}">
              <div class="pointer-fine:hidden">
                <button type="submit" is="custom-button" class="product-card__mobile-quick-buy-button" aria-label="{{ quick_add_label | escape }}">
                  {%- render 'icon' with 'quick-buy-cart' -%}
                </button>
              </div>
              <div class="pointer-coarse:hidden">
                {%- render 'button', type: 'submit', content: quick_add_label -%}
              </div>
            {%- endform -%}
          {%- else -%}
            {%- capture quick_buy_id -%}quick-buy-{{ section.id }}-{{ product.id }}{%- endcapture -%}
            <div class="pointer-fine:hidden">
              <button type="button" aria-controls="{{ quick_buy_id }}" aria-expanded="false" aria-label="{{ quick_add_label | escape }}" is="custom-button" class="product-card__mobile-quick-buy-button">
                {%- render 'icon' with 'quick-buy-cart' -%}
              </button>
            </div>
            <div class="pointer-coarse:hidden">
              {%- render 'button', content: quick_add_label, aria_controls: quick_buy_id -%}
            </div>
            <quick-buy-drawer id="{{ quick_buy_id }}" header-bordered open-from="bottom" handle="{{ product.handle }}" role="region" aria-live="polite" class="quick-buy-drawer drawer">
            </quick-buy-drawer>
          {%- endif -%}
        </div>
      {%- endif -%}
    </div>
  {%- endif -%}

  {%- comment -%}──────────────────────────────────────────────────────
    INFO-SEKTION
    Ordning: leverantör → titel → lagerstatus → stjärnor → pris+köp
  ──────────────────────────────────────────────────────{%- endcomment -%}
  <div class="product-card__info-stack">

    {%- comment -%} LEVERANTÖR {%- endcomment -%}
    {%- if show_vendor and product.vendor != blank -%}
      <span class="product-card__vendor-label">{{ product.vendor }}</span>
    {%- endif -%}

    {%- comment -%} TITEL {%- endcomment -%}
    <a href="{{ product.url }}" class="product-card__title-link" data-instant>{{ product.title }}</a>

    {%- comment -%} LAGERSTATUS {%- endcomment -%}
    <div class="product-card__stock">
      {%- if product.available -%}
        <span class="product-card__stock-dot product-card__stock-dot--in"></span>
        <span class="product-card__stock-text">Finns i lager</span>
      {%- else -%}
        <span class="product-card__stock-dot product-card__stock-dot--out"></span>
        <span class="product-card__stock-text">Slutsåld</span>
      {%- endif -%}
    </div>

    {%- comment -%}
      STJÄRNBETYG — 5 stjärnor, fylls baserat på betyget.
      Färg: #025dc6. Halvstjärna via SVG linearGradient.
      Visar "X.X/5 (antal)" som text bredvid stjärnorna.
    {%- endcomment -%}
    {%- if show_rating and pc_x2 > 0 -%}
      <div class="product-card__stars" aria-label="Betyg {{ pc_rv | round: 1 }} av 5">

        {%- for i in (1..5) -%}
          {%- assign i_x2 = i | times: 2 -%}
          {%- assign i_x2h = i_x2 | minus: 1 -%}
          {%- if pc_x2 >= i_x2 -%}
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path fill-rule="evenodd" clip-rule="evenodd" d="M14.4012 3.17499C13.8719 2.11134 13.0447 1.32715 11.9987 1.32715C10.9539 1.32715 10.1241 2.11 9.58985 3.17284L7.82915 6.69424L7.82642 6.69975C7.77359 6.8076 7.64731 6.9611 7.45702 7.10219C7.26737 7.2428 7.0834 7.31975 6.96547 7.34005L6.96441 7.34023L3.77705 7.86979C2.62694 8.06147 1.66106 8.62118 1.34801 9.6015C1.03519 10.5811 1.49663 11.597 2.31964 12.42L4.79964 14.9C4.89724 14.9976 5.0066 15.1815 5.07518 15.4211C5.14334 15.6592 5.1491 15.8751 5.11856 16.0137L5.11813 16.0156L4.40928 19.0806C4.11373 20.3578 4.21624 21.6259 5.12539 22.2893C6.03398 22.9522 7.27361 22.6647 8.40273 21.9946L11.3871 20.2279L11.389 20.2268C11.5232 20.1494 11.7443 20.0896 12.0037 20.0896C12.265 20.0896 12.4816 20.1502 12.6076 20.2249L15.5979 21.995C16.7284 22.6628 17.9695 22.9555 18.878 22.2932C19.7878 21.6299 19.8854 20.3595 19.5908 19.0812L18.8818 16.0156L18.8814 16.0137C18.8508 15.8751 18.8566 15.6592 18.9248 15.4211C18.9933 15.1815 19.1027 14.9976 19.2003 14.9L21.6785 12.4218L21.6795 12.4208C22.5077 11.5978 22.9706 10.5811 22.6552 9.60014C22.3403 8.62044 21.3726 8.06141 20.2233 7.86985L17.0333 7.33985C16.9166 7.31975 16.73263 7.2428 16.54298 7.10219C16.35269 6.9611 16.22641 6.8076 16.17358 6.69975L16.17085 6.69424L14.4012 3.17499Z" fill="#025dc6"/>
            </svg>
          {%- elsif pc_x2 == i_x2h -%}
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <defs>
                <linearGradient id="hsg-{{ product.id }}-{{ i }}" x1="0" y1="0" x2="1" y2="0">
                  <stop offset="50%" stop-color="#025dc6"/>
                  <stop offset="50%" stop-color="#d1d5db"/>
                </linearGradient>
              </defs>
              <path fill-rule="evenodd" clip-rule="evenodd" d="M14.4012 3.17499C13.8719 2.11134 13.0447 1.32715 11.9987 1.32715C10.9539 1.32715 10.1241 2.11 9.58985 3.17284L7.82915 6.69424L7.82642 6.69975C7.77359 6.8076 7.64731 6.9611 7.45702 7.10219C7.26737 7.2428 7.0834 7.31975 6.96547 7.34005L6.96441 7.34023L3.77705 7.86979C2.62694 8.06147 1.66106 8.62118 1.34801 9.6015C1.03519 10.5811 1.49663 11.597 2.31964 12.42L4.79964 14.9C4.89724 14.9976 5.0066 15.1815 5.07518 15.4211C5.14334 15.6592 5.1491 15.8751 5.11856 16.0137L5.11813 16.0156L4.40928 19.0806C4.11373 20.3578 4.21624 21.6259 5.12539 22.2893C6.03398 22.9522 7.27361 22.6647 8.40273 21.9946L11.3871 20.2279L11.389 20.2268C11.5232 20.1494 11.7443 20.0896 12.0037 20.0896C12.265 20.0896 12.4816 20.1502 12.6076 20.2249L15.5979 21.995C16.7284 22.6628 17.9695 22.9555 18.878 22.2932C19.7878 21.6299 19.8854 20.3595 19.5908 19.0812L18.8818 16.0156L18.8814 16.0137C18.8508 15.8751 18.8566 15.6592 18.9248 15.4211C18.9933 15.1815 19.1027 14.9976 19.2003 14.9L21.6785 12.4218L21.6795 12.4208C22.5077 11.5978 22.9706 10.5811 22.6552 9.60014C22.3403 8.62044 21.3726 8.06141 20.2233 7.86985L17.0333 7.33985C16.9166 7.31975 16.73263 7.2428 16.54298 7.10219C16.35269 6.9611 16.22641 6.8076 16.17358 6.69975L16.17085 6.69424L14.4012 3.17499Z" fill="url(#hsg-{{ product.id }}-{{ i }})"/>
            </svg>
          {%- else -%}
            <svg width="15" height="15" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
              <path fill-rule="evenodd" clip-rule="evenodd" d="M14.4012 3.17499C13.8719 2.11134 13.0447 1.32715 11.9987 1.32715C10.9539 1.32715 10.1241 2.11 9.58985 3.17284L7.82915 6.69424L7.82642 6.69975C7.77359 6.8076 7.64731 6.9611 7.45702 7.10219C7.26737 7.2428 7.0834 7.31975 6.96547 7.34005L6.96441 7.34023L3.77705 7.86979C2.62694 8.06147 1.66106 8.62118 1.34801 9.6015C1.03519 10.5811 1.49663 11.597 2.31964 12.42L4.79964 14.9C4.89724 14.9976 5.0066 15.1815 5.07518 15.4211C5.14334 15.6592 5.1491 15.8751 5.11856 16.0137L5.11813 16.0156L4.40928 19.0806C4.11373 20.3578 4.21624 21.6259 5.12539 22.2893C6.03398 22.9522 7.27361 22.6647 8.40273 21.9946L11.3871 20.2279L11.389 20.2268C11.5232 20.1494 11.7443 20.0896 12.0037 20.0896C12.265 20.0896 12.4816 20.1502 12.6076 20.2249L15.5979 21.995C16.7284 22.6628 17.9695 22.9555 18.878 22.2932C19.7878 21.6299 19.8854 20.3595 19.5908 19.0812L18.8818 16.0156L18.8814 16.0137C18.8508 15.8751 18.8566 15.6592 18.9248 15.4211C18.9933 15.1815 19.1027 14.9976 19.2003 14.9L21.6785 12.4218L21.6795 12.4208C22.5077 11.5978 22.9706 10.5811 22.6552 9.60014C22.3403 8.62044 21.3726 8.06141 20.2233 7.86985L17.0333 7.33985C16.9166 7.31975 16.73263 7.2428 16.54298 7.10219C16.35269 6.9611 16.22641 6.8076 16.17358 6.69975L16.17085 6.69424L14.4012 3.17499Z" fill="#d1d5db"/>
            </svg>
          {%- endif -%}
        {%- endfor -%}

        <span class="product-card__rating-text">
          {{- pc_rv | round: 1 -}}/5
          {%- if pc_rc > 0 %} ({{ pc_rc }}){%- endif -%}
        </span>

      </div>
    {%- elsif show_rating -%}
      {%- capture rating_class -%}{% if mobile_reduced %}hidden sm:flex{% endif %}{%- endcapture -%}
      {%- render 'product-rating', product: product, class: rating_class, display_mode: settings.product_rating_mode -%}
    {%- endif -%}

    {%- comment -%} PRIS + KÖP-KNAPP {%- endcomment -%}
    <div class="product-card__price-row">
      {%- render 'price-list', product: product, text_alignment: 'left' -%}

      {%- if product.available -%}
        {%- assign add_variant_id = product.selected_or_first_available_variant.id -%}
        <button
          type="button"
          class="product-card__buy-btn"
          data-quick-add
          data-variant-id="{{ add_variant_id }}"
          data-product-handle="{{ product.handle }}"
          aria-label="Köp {{ product.title | escape }}">
          <span class="btn-label">Köp</span>
          <svg class="btn-spinner" width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <circle cx="12" cy="12" r="10" stroke="rgba(255,255,255,0.25)" stroke-width="2.5"/>
            <path d="M12 2a10 10 0 0 1 10 10" stroke="#fff" stroke-width="2.5" stroke-linecap="round"/>
          </svg>
          <svg class="btn-check" width="16" height="16" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
            <path d="M4 12l5 5 11-10" stroke="#fff" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </button>
      {%- else -%}
        <span class="product-card__buy-btn product-card__buy-btn--disabled">Slutsåld</span>
      {%- endif -%}
    </div>

    {%- comment -%} FÄRGSVEPARE (bevarad originalfunktionalitet) {%- endcomment -%}
    {%- if settings.product_color_display != 'hide' and show_swatches != false -%}
      {%- assign color_label_list = 'general.label.color' | t | replace: ', ', ',' | downcase | split: ',' -%}
      {%- for color_label in color_label_list -%}
        {%- if product.options_by_name[color_label] != blank -%}
          {%- assign product_option = product.options_by_name[color_label] -%}
          {%- capture name -%}swatch-{{ section.id }}-{{ product.id }}-{{ product_option.position }}{%- endcapture -%}
          <div class="product-card__aside">
            {%- case settings.product_color_display -%}
              {%- when 'count' -%}
                <p class="product-card__color-count text-sm text-subdued">{{- 'product.general.available_colors_count' | t: count: product_option.values.size -}}</p>
              {%- when 'swatch' -%}
                <fieldset class="product-card__swatch-list h-stack {% if settings.color_swatch_style == 'rectangle' %}gap-2.5{% else %}gap-0.5{% endif %}" data-option-position="{{ product_option.position }}">
                  {%- for option_value in product_option.values limit: 4 -%}
                    {%- if forloop.first or product.selected_or_first_available_variant.matched and option_value == product_option.selected_value -%}{%- assign selected = true -%}{%- else -%}{%- assign selected = false -%}{%- endif -%}
                    {%- render 'swatch' with 'color', value: option_value, name: name, size: 'sm', selected: selected -%}
                  {%- endfor -%}
                  {%- if product_option.values.size > 4 %}<a href="{{ product.url }}" data-instant class="color-swatch__view-more {% if settings.color_swatch_style == 'round' %}rounded-full{% endif %} text-xxs text-subdued">+{{ product_option.values.size | minus: 4 }}</a>{%- endif -%}
                </fieldset>
              {%- when 'variant' -%}
                <fieldset class="product-card__variant-list" data-option-position="{{ product_option.position }}">
                  {%- for option_value in product_option.values limit: 4 -%}
                    {%- assign variant_option = 'option' | append: product_option.position -%}
                    {%- assign variant_for_value = product.variants | where: variant_option, option_value | first -%}
                    {%- if forloop.first or variant_for_value.matched and option_value == product_option.selected_value -%}{%- assign selected = true -%}{%- else -%}{%- assign selected = false -%}{%- endif -%}
                    {%- render 'swatch' with 'media', media: variant_for_value.featured_media, value: option_value, name: name, size: 'sm', selected: selected -%}
                  {%- endfor -%}
                  {%- if product_option.values.size > 4 -%}<a href="{{ product.url }}" data-instant class="media-swatch__view-more text-xs text-subdued">+{{ product_option.values.size | minus: 4 }}</a>{%- endif -%}
                </fieldset>
            {%- endcase -%}
          </div>
          {%- break -%}
        {%- endif -%}
      {%- endfor -%}
    {%- endif -%}

  </div>
</product-card>

{%- comment -%}
  ── KÖP-KNAPP: QUICK ADD VIA AJAX ────────────────────────────────────
  Lägger till produkten direkt i varukorgen via /cart/add.js utan att
  öppna drawer eller gå till produktsidan. Använder data-variant-id
  som sätts från Liquid (product.selected_or_first_available_variant).
  Spinner under add → checkmark vid success → återgår till "Köp".
  Triggar både Shopify PubSub och generiska cart:updated/cart:refresh
  events så att tema-komponenter (cart-drawer, cart-count) uppdaterar
  sig automatiskt. Guard-attribut förhindrar dubbla lyssnare när
  snippet renderas flera gånger på samma sida.
{%- endcomment -%}
{%- comment -%}
  ── KÖP-KNAPP: QUICK ADD VIA AJAX ────────────────────────────────────
  Lägger till produkten direkt i varukorgen via /cart/add.js och
  uppdaterar sedan alla cart-relaterade UI-element via section
  rendering API + ett brett spektrum av events så att olika teman
  uppdaterar header-räknare och drawer korrekt utan reload.
{%- endcomment -%}
<script>
  (function () {
    if (document.documentElement.hasAttribute('data-pc-buy-init')) return;
    document.documentElement.setAttribute('data-pc-buy-init', '');

    var SUCCESS_MS = 1800;

    // Sektioner som ska refreshas efter add. Vi gissar de vanligaste
    // namnen — Shopify ignorerar tysta de som inte finns i temat.
    var SECTIONS_TO_REFRESH = [
      'cart-drawer',
      'cart-icon-bubble',
      'cart-live-region-text',
      'mini-cart',
      'header',
      'cart-notification',
      'cart-notification-product'
    ];

    function updateCartCountFallback(cart) {
      // Generisk fallback: leta upp vanliga räknar-element och uppdatera
      // dem manuellt om sections-API:et inte gav oss något.
      var count = cart && typeof cart.item_count === 'number' ? cart.item_count : null;
      if (count === null) return;

      var selectors = [
        '[data-cart-count]',
        '.cart-count',
        '.cart-count-bubble',
        '.cart-link__bubble-num',
        '.site-header__cart-count span',
        '.header__cart-count',
        '#CartCount',
        '#cart-icon-bubble span'
      ];

      selectors.forEach(function (sel) {
        document.querySelectorAll(sel).forEach(function (el) {
          el.textContent = count;
          el.classList.toggle('hidden', count === 0);
          el.classList.toggle('cart-count-bubble--visible', count > 0);
        });
      });
    }

    function replaceSectionHTML(sectionId, newHTML) {
      // Hitta elementet på sidan som matchar sektion-id och ersätt dess
      // innerHTML med den nya. Shopify section rendering API returnerar
      // hela sektionens HTML — vi extraherar innehållet ur shopify-section
      // wrappern innan vi byter.
      var target = document.getElementById('shopify-section-' + sectionId);
      if (target) {
        target.innerHTML = new DOMParser()
          .parseFromString(newHTML, 'text/html')
          .querySelector('#shopify-section-' + sectionId).innerHTML;
        return true;
      }

      // Vissa teman renderar utan shopify-section wrapper. Prova generiskt.
      var alt = document.querySelector('[data-section-id="' + sectionId + '"]')
             || document.querySelector('cart-drawer')
             || document.querySelector('#cart-icon-bubble');
      if (alt && sectionId === 'cart-icon-bubble') {
        alt.innerHTML = newHTML;
        return true;
      }
      return false;
    }

    function refreshCartSections(cart) {
      // Hämta uppdaterade sektioner och uppdatera dem i DOM.
      var sectionsParam = SECTIONS_TO_REFRESH.join(',');
      return fetch('/?sections=' + encodeURIComponent(sectionsParam), {
        headers: { 'Accept': 'application/json' }
      })
        .then(function (r) { return r.ok ? r.json() : {}; })
        .then(function (sections) {
          var anyReplaced = false;
          Object.keys(sections || {}).forEach(function (key) {
            if (sections[key]) {
              if (replaceSectionHTML(key, sections[key])) anyReplaced = true;
            }
          });
          // Fallback om inget byttes — uppdatera räknare manuellt.
          if (!anyReplaced) updateCartCountFallback(cart);
        })
        .catch(function () {
          updateCartCountFallback(cart);
        });
    }

    function dispatchCartEvents(cart) {
      // Shopify Pub/Sub (Dawn och nyare teman).
      try {
        if (window.Shopify && window.Shopify.PubSub && typeof window.Shopify.PubSub.publish === 'function') {
          window.Shopify.PubSub.publish('cart:refresh', { source: 'product-card-quick-add' });
        }
      } catch (e) {}

      // Generiska events som många teman lyssnar pa.
      var eventNames = [
        'cart:updated',
        'cart:refresh',
        'cart:added',
        'cart:build',
        'cart:change',
        'ajaxCart.afterCartLoad',
        'product:added-to-cart'
      ];
      eventNames.forEach(function (name) {
        document.dispatchEvent(new CustomEvent(name, {
          bubbles: true,
          detail: { cart: cart, source: 'product-card-quick-add' }
        }));
        document.body.dispatchEvent(new CustomEvent(name, {
          bubbles: true,
          detail: { cart: cart, source: 'product-card-quick-add' }
        }));
      });

      // jQuery-event för äldre teman.
      if (window.jQuery) {
        try { window.jQuery(document).trigger('cart.requestComplete', cart); } catch (e) {}
        try { window.jQuery(document.body).trigger('added.ajaxProduct'); } catch (e) {}
      }
    }

    document.addEventListener('click', function (e) {
      var btn = e.target.closest('.product-card__buy-btn[data-quick-add]');
      if (!btn) return;
      if (btn.classList.contains('product-card__buy-btn--disabled')) return;
      if (btn.classList.contains('is-loading') || btn.classList.contains('is-success')) return;

      e.preventDefault();

      var variantId = btn.getAttribute('data-variant-id');
      if (!variantId) return;

      btn.classList.add('is-loading');

      fetch('/cart/add.js', {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          'Accept': 'application/json',
          'X-Requested-With': 'XMLHttpRequest'
        },
        body: JSON.stringify({
          items: [{ id: parseInt(variantId, 10), quantity: 1 }]
        })
      })
        .then(function (res) {
          if (!res.ok) return res.json().then(function (err) { throw err; });
          return res.json();
        })
        .then(function () {
          return fetch('/cart.js', { headers: { 'Accept': 'application/json' } })
            .then(function (r) { return r.json(); });
        })
        .then(function (cart) {
          // Uppdatera DOM via sections + skicka events.
          return refreshCartSections(cart).then(function () {
            dispatchCartEvents(cart);
            return cart;
          });
        })
        .then(function () {
          btn.classList.remove('is-loading');
          btn.classList.add('is-success');
          setTimeout(function () { btn.classList.remove('is-success'); }, SUCCESS_MS);
        })
        .catch(function (err) {
          btn.classList.remove('is-loading');
          console.warn('[product-card] quick add failed:', err);
          var label = btn.querySelector('.btn-label');
          if (label) {
            var original = label.textContent;
            label.textContent = 'Fel';
            setTimeout(function () { label.textContent = original; }, 1600);
          }
        });
    });
  })();
</script>